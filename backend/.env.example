# Copy to .env and adjust. Defaults match backend/docker-compose.yml (Postgres on host port 5433).

SECRET_KEY=change-me-to-a-long-random-string
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=mydb
DB_USER=admin
DB_PASSWORD=secret
DB_HOST=127.0.0.1
DB_PORT=5433
